import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ktcl.app',
  appName: 'ktcl-bus-card',
  webDir: 'dist/ktcl-bus-card/browser'
};

export default config;
